/* ============================================
   FightHub — Public site configuration
   The Supabase anon (publishable) key is designed to be public: it only
   grants what the database's Row Level Security policies allow.
   NEVER put the service_role key or any API key (OpenRouter, etc.) here.
   Leave both values empty to run in local (browser-only) mode.
   ============================================ */

window.FIGHTHUB_CONFIG = {
  supabaseUrl: 'https://ewfuhrlgdivwtdremkdv.supabase.co',
  supabaseAnonKey: 'sb_publishable_j9JDQxPwdCHhrw93Wn3j-Q_BKVpTCk1'
};
